# FLiPIT refine — pin only

Not a traveler. Not product law. Proposed cuts for PR discussion.
Parent tip: `d440ba3` on `docs/autonest-host` / PR 133.

## Scope
Polish what already shipped on the FLiPIT host. No new surfaces. No picker, calc, chevron, or bed.

## Proposed cuts (draft)

1. Center HUD inner seats — keep the open-editor inner widths and gaps after Check/X. Outer pin already locked.
2. Count well — fixed tabular 999 seat, digits centered, no runtime grow.
3. Resize arc inset — 25px inside the window on left and right so the drag arc is not cropped.
4. Margin bands + eye — bands stay inside each isolated blank; eye / eye-off on the MARGIN label right edge.
5. Local Look path — `/flipit` is Clerk-wrapped; keep 8092 file serve as the Look URL until a later auth cut.
6. Sweep — anything already built that still reads off after Owner Look. No new ideas on this PR.

Strike or add here before the traveler is written.
