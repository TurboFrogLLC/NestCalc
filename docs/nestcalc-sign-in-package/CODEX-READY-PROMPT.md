# CODEX-READY-PROMPT — NestCalc Sign-in (DRAFT · non-executable)

**Do not run until a dedicated NestCalc GOAL freezes this package.**  
**Not valid for Flow NC-20260810-472606a4.**

---

After a future freeze adopts `docs/nestcalc-sign-in-package/` as hard authority:

```text
Repo: TurboFrogLLC/NestCalc
Authority: docs/nestcalc-sign-in-package/REFERENCE-PROTOTYPE.html + DESIGN-TOKENS.md
Fail closed: page #E8E8EC; card #0E0C14; primary #538BEC; Nest slate + italic Calc blue; no amber #fbbf24.
Request-access: only remove if GOAL explicitly authorizes policy change; otherwise restyle or leave per GOAL text.
Allowed Files (expected — confirm against frozen GOAL):
  src/app/sign-in/[[...sign-in]]/page.tsx
  src/lib/clerkAppearance.ts
  (+ RequestAccessForm only if GOAL names it)
Do not touch: proxy, middleware, secrets, PWA, calculator/G-code engines, active dark-shell Flow files beyond explicit Allowed Files.
Proof: visual sign-in desktop/mobile; authenticated flows still green; no public sign-up footer links.
```

This file does not authorize implementation by itself.
