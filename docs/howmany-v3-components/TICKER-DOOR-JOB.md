# Ticker door job

Docs only until Cut stamps. Child of `feat/v3-c1-count-r1-join`. Not `main`.

Product is FLiPIT. Host for this job stays `docs/howmany-v3-components/COMPOSITION-FLIPIT-v3.html`.

## Pin — HTML now, React later

This door lands on the HTML composition host.
Do not `npx shadcn add` into that file. Do not steal-look as the end state.

After Plants 1–3 are good enough, a **later PR** ports the whole V3 host to a Next/React route so real shadcn components can be composed. That port is not this traveler. Do not plan it here.

Law for this job: [status/flipit-ticker-door.md](https://github.com/TurboFrogLLC/wReckless-Robot/blob/main/status/flipit-ticker-door.md) Closed sibling. Not `#120` undo-2 table. Not `status/flipit-look-ticker.md` Auto-Size-opens-FLiPIT line.

## Order (do not skip)

1. Start-branch — this child branch off 119 tip.
2. Plant 1 Look — persistent cluster + ghost rail + dead chips. Dump stays.
3. Plant 2 Session — confirm row + grow-up writes + `resetManualKeepRemnant`. Then tear dump.
4. Plant 3 Earned chip — one slot Auto-Size empty / FLiPIT loaded.
5. Send for review → Wait → Inspection → Merge into 119 → Close.

## Plant 1 proof

Closed: `[-−90][+−90][ticker][›][calc]`
Open rail does not clip ±90 or calc. History icons off the cluster (`corner-up-left` / `corner-up-right`). Dump still works underneath.

## Plant 2 proof

Gap/margin edit from ticker moves count. Reset keeps the blank. Dump torn only after writes work.

## Plant 3 proof

One rail slot. Auto-Size does not open FLiPIT. After load that slot is the FLiPIT strip.

## Out

React/shadcn host port. HexNest chrome. Bed lock. One-surface mobile. Cut sheet. Presets. Radial. `#116` `#117` `#118`. Merge to `main`.
