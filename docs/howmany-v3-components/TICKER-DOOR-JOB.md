# Ticker door job

Docs only until Cut stamps. Child of `feat/v3-c1-count-r1-join`. Not `main`.

Product is FLiPIT. Host for this job stays `docs/howmany-v3-components/COMPOSITION-FLIPIT-v3.html`.

## Pin — HTML now, React later

This door lands on the HTML composition host.
Do not `npx shadcn add` into that file. Do not steal-look as the end state.

After Plants 1–3 are good enough, a **later PR** ports the whole V3 host to a Next/React route so real shadcn components can be composed. That port is not this traveler. Do not plan it here.

Law for this job: [status/flipit-ticker-door.md](https://github.com/TurboFrogLLC/wReckless-Robot/blob/main/status/flipit-ticker-door.md) Closed sibling. Icons table: NestCalc #120 `docs/howmany-v3-components/TICKER-DOOR-MARK.md`. Not `#120` undo-2 table. Not `status/flipit-look-ticker.md` Auto-Size-opens-FLiPIT line.

## Order (do not skip)

1. Start-branch — this child branch off 119 tip.
2. Plant 1 Look — persistent cluster + ghost rail + dead chips. Dump stays.
3. Plant 1b Tuck-under — closed is travel, open is edit. ±90 tuck under ticker. Calc leaves. Chevron parked. Icon rail. Handle keep-out.
3c. Plant 1c Glyphs — #120 lucides + outline chips + no ticker HUD pour.
4. Plant 2 Session — confirm row + grow-up writes + `resetManualKeepRemnant`. Then tear dump.
5. Plant 3 Earned chip — one slot Auto-Size empty / FLiPIT loaded.
6. Send for review → Wait → Inspection → Merge into 119 → Close.

## Plant 1 proof

Closed: `[−90][+90][ticker][›][calc]`
Open rail does not clip ±90 or calc. History icons off the cluster (`corner-up-left` / `corner-up-right`). Dump still works underneath.

## Plant 1b proof

Closed stays travel: `[−90][+90][ticker][›][calc]`.
Open is edit only: `[ticker][›][part][gap][margin][eraser]`.
±90 slide under the ticker and lose hit. Calc leaves. Chevron stays glued to the ticker right edge (boxed hit, thicker stroke). Rail is four icon chips. No Blank chip. Rail chips do not open the HUD field card. Ticker click does not open the rail. Motion ~300–400ms ease-out; rail clips out of the ticker back. Open pack must not cover `#lb-hit-corner` or `#lb-count`; translate the pack left if it would. `#lb-count` stays. Idle cluster paint one step darker than the grid; no yellow on idle ticker.

## Plant 1c proof

Lucide only, from #120 Icons table:
- Part `rectangle-vertical`
- Gap `between-horizontal-end`
- Margin `align-start-vertical`
- Reset `eraser`
- Door `chevron-last` / `chevron-first`
No `bolt`. No split-square. No hash. Rail chips and chevron are outline only (no gray fill). Ticker `dblclick` must not call `__howManyOpenField`. Double-click is idle until Plant 2 grow-up. Push origin before stamp.

## Plant 2 proof

Gap/margin edit from ticker moves count. Reset keeps the blank. Dump torn only after writes work.

## Plant 3 proof

One rail slot. Auto-Size does not open FLiPIT. After load that slot is the FLiPIT strip.

## Out

React/shadcn host port. HexNest chrome. Bed lock. One-surface mobile. Cut sheet. Presets. Radial. `#116` `#117` `#118`. Merge to `main`.
