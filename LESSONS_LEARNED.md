# LESSONS_LEARNED.md - NestCalc

## Purpose

Reusable lessons from merged PR closeout breakdowns for NestCalc. Advisory
execution memory only — does not override `NestCalc_Build_Spec.md`, `AGENTS.md`,
or explicit human direction.

## How To Use

Before implementation or PR closeout:

1. Read `NestCalc_Build_Spec.md`.
2. Read this file and identify relevant `L-nestcalc-*` labels.
3. Treat matching lessons as preflight checks and regression risks.
   Skip retired rows.

## Lesson Index

| Label | Source | Lesson | Applies when |
|---|---|---|---|
| `L-nestcalc-next16-serwist-turbopack` | [PR #1](https://github.com/TurboFrogLLC/NestCalc/pull/1) | Next.js 16 production builds default to Turbopack; PWA service workers should use @serwist/turbopack (route handler + SerwistProvider at /serwist/sw.js), not @serwist/next/webpack. With src-dir, set swSrc to src/app/sw.ts. | Scaffolding or upgrading NestCalc/Next.js 16 PWAs with Serwist. |
| `L-nestcalc-socket-transitive-triage` | [PR #1](https://github.com/TurboFrogLLC/NestCalc/pull/1) | Socket obfuscation alerts on eslint-config-next and @tailwindcss/postcss transitive dev deps are usually false positives; document dev-only triage in docs/SOCKET_SECURITY.md and post @SocketSecurity ignore comments after review — do not drop Next.js lint alignment to silence alerts. | Socket PR alerts on NestCalc or other Next.js eslint-config-next scaffolds. |
| `L-nestcalc-postcss-override` | [PR #1](https://github.com/TurboFrogLLC/NestCalc/pull/1) | Next 16 may nest postcss below GHSA-qx2v-qp2m-jg93; add package.json overrides.postcss ^8.5.10 and re-run npm audit before merge — npm audit fix --force downgrades Next and must be avoided. | npm audit reports moderate postcss via next on NestCalc or similar Next 16 repos. |
| `L-nestcalc-allowscripts-pin` | [PR #1](https://github.com/TurboFrogLLC/NestCalc/pull/1) | On npm v11+, pin allowScripts entries for @swc/core, esbuild, sharp, and unrs-resolver after reviewing install scripts; use npm approve-scripts --allow-scripts-pending to discover pending packages. | Initial Next.js 16 scaffold or dependency refresh shows allow-scripts warnings. |
| `L-nestcalc-ios-safe-area-standalone` | [PR #2](https://github.com/TurboFrogLLC/NestCalc/pull/2) | iOS standalone PWAs with viewportFit cover and black-translucent status bar need body padding-top: env(safe-area-inset-top, 0px) to clear the notch/Dynamic Island without changing internal layout spacing. | Fixing status bar overlap on iPhone home-screen PWAs where viewport-fit=cover is already set. |
| `L-nestcalc-serwist-ios-offline` | [PR #2](https://github.com/TurboFrogLLC/NestCalc/pull/2) | For Serwist + Next.js 16 offline on iOS when the origin/tunnel dies, explicitly precache / and /~offline, set navigateFallback to /, broaden document fallbacks to request.mode === navigate, and disable navigationPreload to avoid delayed offline responses. | Hardening NestCalc or similar Next.js 16 Serwist turbopack PWAs for shop-floor offline use on iPhone. |
| `L-nestcalc-clerk-pwa-public-routes` | [PR #9](https://github.com/TurboFrogLLC/NestCalc/pull/9) | When adding Clerk `clerkMiddleware`, keep `/~offline` (and `/sign-in`, `/sign-up`) in `createRouteMatcher` public routes and include `/__clerk/:path*` in the proxy `config.matcher` after `/(api|trpc)(.*)` — otherwise Serwist offline fallback and Clerk dev proxy break behind the auth gate. | Integrating Clerk auth into NestCalc or other Next.js 16 Serwist PWAs. |
| `L-nestcalc-env-example-gitignore` | [PR #10](https://github.com/TurboFrogLLC/NestCalc/pull/10) | Repos that gitignore `.env*` must add `!.env.example` before committing a Clerk `.env.example` template — otherwise `git add` silently skips the file and onboarding docs reference a missing committed artifact. | Adding `.env.example` to NestCalc or other Next.js repos with a broad `.env*` gitignore rule. |
| `L-nestcalc-clerk-header-userbutton-only` | [PR #10](https://github.com/TurboFrogLLC/NestCalc/pull/10) | When Clerk `clerkMiddleware` already redirects unsigned users to `/sign-in`, header `AuthControls` should render `UserButton` only inside `<Show when="signed-in">` — signed-out modal Sign in/Sign up buttons never mount on protected `/` and add header clutter. | Polishing Clerk header UX on NestCalc or similar server-gated Next.js apps. |
| `L-nestcalc-clerk-appearance-shared` | [PR #10](https://github.com/TurboFrogLLC/NestCalc/pull/10) | Share a single `nestcalcClerkAppearance` module (dark `#09090b`, gold `#fbbf24` primary) across `<SignIn />`, `<SignUp />`, and `UserButton`, and set `forceRedirectUrl="/"` on auth pages so post-login returns to the calculator without duplicating theme props. | Theming Clerk prebuilt components to match NestCalc shop-floor styling. |
| `L-nestcalc-clerk-input-contrast` | [PR #11](https://github.com/TurboFrogLLC/NestCalc/pull/11) | When Clerk sign-in inputs blend into NestCalc's dark page, strengthen both `variables` (`colorInputBackground`, `colorInputText`, `colorNeutral`) and `elements.formFieldInput` with explicit `borderColor` plus a gold `&:focus` ring (`#fbbf24`) — variables alone are often too subtle on `#09090b` backgrounds. | Fixing low-contrast Clerk auth inputs on NestCalc or similar dark-themed sign-in/sign-up pages. |
| `L-nestcalc-clerk-card-background` | [PR #12](https://github.com/TurboFrogLLC/NestCalc/pull/12) | Clerk `variables.colorBackground` styles the sign-in/sign-up card container, not the page wrapper — on NestCalc's dark `bg-[var(--background)]` pages, set `colorBackground` to `#ffffff` (plus `colorForeground`/`colorInput` for dark-on-white text) and keep the outer page dark; do not reuse `#09090b` for `colorBackground` or the card disappears into the background. | Fixing black-on-black Clerk login cards on NestCalc or similar dark-page auth routes. |
| `L-nestcalc-ios-quickbar-touch` | [PR #13](https://github.com/TurboFrogLLC/NestCalc/pull/13) | iOS keyboard accessory bars need both `onMouseDown` and `onTouchStart` preventDefault on preset buttons; mouse-only handling lets Safari blur the focused input before `onClick` fires. | Adding floating quick-value or fraction preset bars above the iOS keyboard in NestCalc numeric inputs. |
| `L-nestcalc-landscape-overflow-qa` | [PR #14](https://github.com/TurboFrogLLC/NestCalc/pull/14) | Phone landscape layouts that forbid scrolling should still use SVG label `Math.max` guards and be manually checked on shortest landscape viewports — `overflow-hidden` on input columns can silently clip margin controls. | Adding NestCalc phone-landscape split layouts with `100dvh` and no-scroll constraints. |
| `L-nestcalc-split-layout-css` | [PR #15](https://github.com/TurboFrogLLC/NestCalc/pull/15) | Tailwind v4 @custom-variant does not reliably compile comma-OR media queries (landscape-phone plus desktop); split layouts that must apply to both phone landscape and min-width breakpoints should use plain globals.css classes with an explicit @media ((orientation: landscape) and (max-height: 500px)), (min-width: 768px) block instead of duplicated utility prefixes. | Adding NestCalc or Tailwind v4 responsive split layouts that combine phone-landscape and desktop two-column shells. |
| `L-nestcalc-gated-signin-no-signup` | [PR #17](https://github.com/TurboFrogLLC/NestCalc/pull/17) | When removing NestCalc public sign-up, delete the route, redirect `/sign-up` in `proxy.ts`, remove `signUpUrl` from `ClerkProvider`/middleware, hide Clerk footer sign-up links in appearance, and update `README.md` + `TUNNEL_SETUP.md` in the same PR — stale docs leave operators configuring dead env vars. | Gating NestCalc or similar Clerk apps behind approval-only access with no public registration. |
| `L-nestcalc-access-request-rate-limit` | [PR #17](https://github.com/TurboFrogLLC/NestCalc/pull/17) | Public NestCalc access-request forms on `/sign-in` need IP/email throttling before Resend sends — honeypot and field caps alone do not stop quota exhaustion. | Adding or reviewing NestCalc Request Access email flows on public auth routes. |
| `L-nestcalc-playwright-clerk-boot` | [PR #18](https://github.com/TurboFrogLLC/NestCalc/pull/18) | NestCalc Playwright proof needs Clerk test-mode `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` and `CLERK_SECRET_KEY` in `.env.local` even for public offline/manifest tests — `playwright.config.ts` starts `next start`, which boots behind `clerkMiddleware` and fails without valid keys. | Adding or running NestCalc Playwright public or auth E2E suites. |
| `L-nestcalc-e2e-split-layout-locator` | [PR #18](https://github.com/TurboFrogLLC/NestCalc/pull/18) | NestCalc split-layout renders duplicate `Parts =` summaries in hidden input chrome and the visible preview header — Playwright strict assertions must scope to `.nestcalc-split-preview-header` (or another unique visible region), not bare `getByText("Parts =")`. | Writing or updating NestCalc Playwright setup/auth assertions on the calculator shell. |
| `L-nestcalc-playwright-auth-setup-order` | [PR #18](https://github.com/TurboFrogLLC/NestCalc/pull/18) | Playwright authenticated E2E must not `test.skip` on missing `playwright/.clerk/user.json` at collection time — the `setup` project dependency runs after collection, so check storage state in `beforeAll` or inside the test instead. | Adding or debugging NestCalc Clerk auth Playwright setup/authenticated projects. |
| `L-nestcalc-grok-review-role-separation` | [NanoTate PR #24](https://github.com/TurboFrogLLC/NanoTate/pull/24) | Keep Grok Build in the review layer by default: findings come back to chat for triage, then accepted items become scoped follow-up work instead of silently expanding the active implementation goal. | Running Grok Build or GitHub review on NestCalc PRs. |
| `L-nestcalc-goal-grilling-authority-sync` | [NanoTate PR #24](https://github.com/TurboFrogLLC/NanoTate/pull/24) | Repo-local goal-prep skills should be wired in all authority surfaces — skill file, `AGENTS.md`, `docs/WORKFLOW.md`, and skill recommendations — with identical stop conditions (no human questions, flagged decisions, stop before commit/CLI prompt). | Adding or updating NestCalc repo-local planning skills or autonomous goal-prep workflow docs. |
| `L-nestcalc-readonly-subagent-model` | [NanoTate PR #24](https://github.com/TurboFrogLLC/NanoTate/pull/24) | When documenting read-only sub-agent lanes, state the permitted model (`gpt-5.4-mini`) in the skill, workflow doc, `AGENTS.md`, and skill recommendations in the same pass — partial updates leave orchestrators guessing which model to spawn. | Defining autonomous goal-prep or evidence-gathering sub-agent constraints in NestCalc governance docs. |
| `L-nestcalc-skill-frontmatter-yaml-quote` | [NanoTate PR #24](https://github.com/TurboFrogLLC/NanoTate/pull/24) | `SKILL.md` frontmatter description values containing `: ` must be quoted YAML scalars — unquoted plain scalars can break loaders and prevent repo-local skill discovery. | Authoring or reviewing repo-local `.agents/skills/*/SKILL.md` frontmatter in NestCalc. |
| `L-nestcalc-codex-stale-sha-guard` | [NanoTate PR #15](https://github.com/TurboFrogLLC/NanoTate/pull/15) | Post a single `@codex review` at final PR HEAD; when triaging Codex inline threads, compare each thread's reviewed commit SHA to current HEAD and ignore, resolve, or defer stale-SHA threads already fixed by later commits. | Running PR review assistants, Codex review, Grok Build intake, or babysit passes on multi-commit NestCalc PRs. |
| `L-nestcalc-pr-branch-main-sync` | [PR #20](https://github.com/TurboFrogLLC/NestCalc/pull/20) | Sync feature branches with main before merge when overlapping fixes (e.g. PR #19 locator) already landed — stale bases duplicate commits and cause LESSONS_LEARNED.md / shared-file merge conflicts. | Opening or merging NestCalc PRs whose branch predates recent main merges touching the same files. |
| `L-nestcalc-goal-required-docs-commit` | [PR #21](https://github.com/TurboFrogLLC/NestCalc/pull/21) | When GOAL.md lists planning docs as required reading, commit those docs in a separate docs commit before or alongside goal handoff — do not leave them untracked while implementation proceeds. | Preparing NestCalc GOAL.md handoffs that reference AutoNest or other planning markdown under docs/. |
| `L-nestcalc-session-boundary-test-surface` | [PR #21](https://github.com/TurboFrogLLC/NestCalc/pull/21) | Nest session refactors should prove behavior parity via session-level tests plus storage migration tests, not only helper-level math tests. | Extracting manual nest session logic from NestCalcApp into a domain module. |
| `L-nestcalc-autonest-app-state-v3` | [PR #22](https://github.com/TurboFrogLLC/NestCalc/pull/22) | AutoNest foundation should introduce a versioned wrapped app-state key (`nestcalc-app-state-v3`) with lazy load from flat `nestcalc-state-v2`/v1, and keep `useNestInputs()` as a manual-input adapter over `useNestAppState()` so NestCalcApp behavior stays unchanged. | Adding AutoNest mode state, settings persistence, or storage migration before engine/UI work in NestCalc. |
| `L-nestcalc-autonest-not-ready-result` | [PR #22](https://github.com/TurboFrogLLC/NestCalc/pull/22) | Before the AutoNest engine exists, return an explicit `not-ready` placeholder result with `bestUniform` from `calculateNest` — never fabricate trim-line, two-group, or preview geometry in foundation passes. | Adding AutoNest result vocabulary or session wrappers without implementing the packing engine. |
| `L-nestcalc-autonest-pure-engine-module` | [PR #23](https://github.com/TurboFrogLLC/NestCalc/pull/23) | AutoNest engine work should stay in a pure domain module (`autoNestEngine.ts`), reuse `calculateNest`/`partsInDimension` for uniform helpers, and wire only through `createNestSession()` — never fold engine logic into `nestcalc.ts` or UI components in engine-only goals. | Implementing AutoNest packing/engine slices before UI or preview work in NestCalc. |
| `L-nestcalc-autonest-thin-blank-guard` | [PR #23](https://github.com/TurboFrogLLC/NestCalc/pull/23) | AutoNest usefulness fallback needs a deterministic thin-blank guard documented in code and tests (e.g. `max(smallestPartSide, 2×largestClampMargin)`); without it, two-group results can return impractically thin trim blanks. | Adding AutoNest two-group trim-line packing with fallback-to-uniform behavior. |
| `L-nestcalc-autonest-minimal-ui-activation` | [PR #24](https://github.com/TurboFrogLLC/NestCalc/pull/24) | Minimal AutoNest UI activation should wire useNestAppState + createNestSession() and gate rotates via session.controls.manualRotationLocked (disabled + handler guard) without changing NestGrid preview or engine modules. | Activating AutoNest mode in NestCalc calculator UI after engine and app-state foundation merges. |
| `L-nestcalc-rotate-aria-label-e2e` | [PR #24](https://github.com/TurboFrogLLC/NestCalc/pull/24) | Playwright rotate-button locators need explicit aria-label on buttons (Rotate Part 90°, Rotate Rem 90°) when visible text differs from accessible name — getByRole({ name }) matches aria-label, not inner text alone. | Adding or updating NestCalc Playwright locators for rotate controls or other buttons with abbreviated visible labels. |
| `L-nestcalc-autonest-search-budget-guard` | [PR #25](https://github.com/TurboFrogLLC/NestCalc/pull/25) | AutoNest performance guards on the synchronous render path should preflight-estimate two-group split candidates before findBestTwoGroupCandidate(), return an explicit search-budget-exceeded fallback to bestUniform, and cover pathological ratios in engine tests — never silently clamp dimensions. | Adding AutoNest engine performance or UI-lockup guards after minimal UI activation in NestCalc. |
| `L-nestcalc-autonest-settings-gear-ui` | [PR #26](https://github.com/TurboFrogLLC/NestCalc/pull/26) | AutoNest settings gear UI should wire AutoNestSettings through useNestAppState().setState on the existing v3 persistence path, keep the panel hidden-by-default with aria-expanded/aria-controls, and extend authenticated Playwright for gear open, margin edit, override reveal, and compact result wording — without touching engine or storage schema. | Exposing AutoNest clamp/margin settings in the calculator UI after engine and minimal activation merges. |
| `L-nestcalc-autonest-computed-preview` | [PR #27](https://github.com/TurboFrogLLC/NestCalc/pull/27) | AutoNest computed preview should use a separate component gated on autoNest.status === computed, keep fallback/not-ready on NestGrid, and align the preview header Parts/grid labels with computedAutoNest totals — never show manual session.manual.result counts beside the two-group visualization. | Adding AutoNest two-group preview visualization after settings gear and performance guard merges. |
| `L-nestcalc-autonest-trim-edge-policy` | [PR #29](https://github.com/TurboFrogLLC/NestCalc/pull/29) | When AutoNest geometry and operator mental model disagree, ship explicit selectable trim-edge policies (open/shared/full) instead of silently changing full-per-blank margins or treating ranking as broken. | Adding or changing AutoNest two-group margin, trim, or clamp-envelope behavior. |
| `L-nestcalc-autonest-shared-trim-one-blank` | [PR #29](https://github.com/TurboFrogLLC/NestCalc/pull/29) | Apply shared internal trim clearance entirely to the non-origin blank leading edge so achievedMargins stay geometric and operators can explain the asymmetric blank. | Implementing shared or kerf-like clearance between two AutoNest trim blanks. |
| `L-nestcalc-margin-input-blur-noop` | [PR #30](https://github.com/TurboFrogLLC/NestCalc/pull/30) | When margin fields display effective fallback values but store nullable overrides, suppress unchanged NumberInput blur commits or no-op session updates when parsed value equals the displayed effective value. | AutoNest or Manual margin NumberInput binds to effective values while nullable overrides inherit a global margin. |
| `L-nestcalc-autonest-fallback-preview-scope` | [PR #30](https://github.com/TurboFrogLLC/NestCalc/pull/30) | AutoNest fallback preview rewiring is a distinct product wave; preserve explicit NestGrid fallback policy in goal out-of-scope until a new goal authorizes it. | Review suggests feeding fallback preview from AutoNest margins while GOAL.md preserves NestGrid for fallback/not-ready states. |
| `L-nestcalc-governance-harden-scope-freeze` | [PR #31](https://github.com/TurboFrogLLC/NestCalc/pull/31) | When hardening governance contracts to Superbrain parity, freeze the CLI command surface and MODE token unless the goal names them; capture executable-gate gaps (handoff revalidate, Flow ID bind) as residual risks with follow-up goals instead of expanding scope mid-PR. | NestCalc governance harden, MODE promotion, or Superbrain baseline alignment waves. |
| `L-nestcalc-b3b4-style-not-stage-codes` | [PR #31](https://github.com/TurboFrogLLC/NestCalc/pull/31) | RETIRED. Old rule forbade NestCalc B1–B5 so SuperBrain B3/B4 would not be copied as NestCalc stations. B5 is now implement, the last production station before B6 QC. Do not treat the old ban as law. | Historical only. See `L-nestcalc-b5-is-implement`. |
| `L-nestcalc-b5-is-implement` | [PR #71](https://github.com/TurboFrogLLC/NestCalc/pull/71) | B5 is implement, the last production station before B6 QC. Job closes at B9. SuperBrain B3/B4 are not NestCalc stations. | Writing NestCalc WORKFLOW or AGENTS station maps. |
| `L-nestcalc-bootstrap-fixture-not-live-goal` | [PR #31](https://github.com/TurboFrogLLC/NestCalc/pull/31) | Bootstrap metadata exceptions must be tested with a fixture goal title, never the live product GOAL.md, so governance waves cannot couple tests to AutoNest or other active product goal state. | Writing or changing NestCalc governance bootstrap exception tests or advisory MODE behavior. |
| `L-nestcalc-gcode-contract-freeze-before-relax` | [PR #33](https://github.com/TurboFrogLLC/NestCalc/pull/33) | A residual that relaxes fail-closed NC safety (mode/unit gates, G53 rejection, omitted-axis emission) is a product-contract change; freeze GOAL.md and ShopHelpers_Spec before implementation, not via an in-place test rewrite. | G-code residual, dialect pass-through, or any change that weakens fail-closed rotation/validation contracts. |
| `L-nestcalc-gcode-pass-through-vs-coordinate-state` | [PR #33](https://github.com/TurboFrogLLC/NestCalc/pull/33) | When rewriting a coordinate scanner for dialect pass-through, keep modal endpoint reconstruction and coordinate-state rejection as separate hard gates from non-target token opacity—passing ACS control text must not imply rewriting G53 or dropping omitted-axis emission. | Lexer/pass-through work on ACS or shop dialect text inside G-code rotation. |
| `L-nestcalc-review-defer-unauthorized-contract` | [PR #33](https://github.com/TurboFrogLLC/NestCalc/pull/33) | Reproduce Codex geometry/safety findings on residual HEAD before auto-fix; if the residual itself lacks GOAL authority, defer code fixes to Codex CLI with refreshed authority rather than patching an unauthorized contract in review. | pr-review-assistant triage when PR changes conflict with frozen GOAL/spec contracts. |
| `L-nestcalc-path-b-goal-freeze-first` | [PR #33](https://github.com/TurboFrogLLC/NestCalc/pull/33) | When a residual chooses Path B (new product contract), freeze GOAL.md and the product spec in goal-memory-only commits before safety fixes so review judges the frozen authority, not the superseded wave. | Residual or follow-up PRs that intentionally change a previously frozen fail-closed contract. |
| `L-nestcalc-rereview-after-path-b-fix` | [PR #33](https://github.com/TurboFrogLLC/NestCalc/pull/33) | After Request Changes on unauthorized NC contract drift, re-run pr-review-assistant only after both the goal freeze and P1 fix commits exist; resolve prior threads against the new HEAD SHA with explicit repro evidence. | Second-pass /pr-review-assistant runs after Codex CLI lands goal-memory + fix commits. |
| `L-nestcalc-fill-unit-session-convert` | [PR #40](https://github.com/TurboFrogLLC/NestCalc/pull/40) | When a G-code (or other) bridge writes calculator unit, reuse session unit helpers (toggleNestSessionUnit / convert-then-overwrite) so remnant, gaps, margins, and AutoNest dimensioned settings are not silently re-tagged without conversion. | Implementing or reviewing NestCalc fills/imports that set NestInputs.unit alongside part dimensions. |
| `L-nestcalc-fill-clear-part-link` | [PR #40](https://github.com/TurboFrogLLC/NestCalc/pull/40) | When importing independent part W×H into NestCalc manual inputs, clear partLinked when spans are unequal so updateManualField cannot later collapse both dimensions. | Bridging external sizes into NestCalc partWidth/partHeight while partLinked may be true. |
| `L-nestcalc-path-a-goal-impl-split` | [PR #40](https://github.com/TurboFrogLLC/NestCalc/pull/40) | Path A G-code fill goals should keep goal-memory commits separate from implementation, treat auth e2e as residual debt, and land session-helper fixes for unit/link invariants before merge-ready closeout. | Closing out NestCalc Path A G-code or calculator-bridge goals that exclude authenticated Playwright from required proof. |
| `L-nestcalc-module-accent-isolation` | [PR #43](https://github.com/TurboFrogLLC/NestCalc/pull/43) | When module accents override --accent, move factual counts/status chrome to a stable token such as --data-accent (and SVG labels to non-module tokens like --part-stroke) so Calculator/G-code accents cannot recolor AutoNest truth or cross-module status. | Redesigning NestCalc module chrome that scopes --accent per Calculator/G-code without changing packing or status semantics. |
| `L-nestcalc-expand-collapse-all-breakpoints` | [PR #43](https://github.com/TurboFrogLLC/NestCalc/pull/43) | G-code full-expand must apply stage collapse endpoints at every layout that can expand — desktop width:0/padding:0 alone leaves portrait mobile stages visually hidden but still tall; pair hide with height release for all expanded viewports when AC requires released space. | Adding full-expand panel modes with shared stage/sheet layouts across mobile portrait and desktop breakpoints. |
| `L-nestcalc-manifest-direct-authority-pins` | [PR #78](https://github.com/TurboFrogLLC/NestCalc/pull/78) | validate_manifest must require glossary, traveler, packslip, and Non-conformance Report template by direct path, not only required_paths, so deleting a pin still fails closed. | Changing NestCalc governance manifest pins or validate_manifest required-authority checks. |
| `L-nestcalc-lifecycle-full-word-qc-release` | [PR #78](https://github.com/TurboFrogLLC/NestCalc/pull/78) | Lifecycle recipe documents must use full-word Quality Control and Release from the glossary. Numeric station identity and retired land-ladder vocabulary do not belong in those two files. | Rewriting docs/governance/goal-lifecycle-contract.md or docs/governance/README.md lifecycle recipe. |
| `L-nestcalc-ncmr-blank-not-merge-block` | [PR #78](https://github.com/TurboFrogLLC/NestCalc/pull/78) | A freeze NCMR with blank Disposition does not block Quality Control or Release of a redo on the same branch. Keep the report posted; do not invent a disposition. | Inspecting or releasing a NestCalc redo after a Non-conformance Report on an earlier freeze. |

## Maintenance Rules

- Append new rows only from final PR closeout breakdowns or human-approved retros.
- Preserve stable `L-nestcalc-*` labels after merge; never rename existing labels.
- Skip duplicate labels when appending.
- Retired rows stay in the index. Do not apply them.
